{
    "name": "icrosschain-solana-router",
    "version": "1.1.1",
    "scripts": {
        "build": "anchor build",
        "test": "anchor test",
        "test-skip-deploy": "anchor test --skip-deploy",
        "deploy": "anchor deploy"
    },
    "dependencies": {
        "@project-serum/anchor": "^0.23.0",
        "@solana/spl-token": "^0.2.0",
        "dotenv": "^16.0.1"
    },
    "devDependencies": {
        "@types/chai": "^4.3.0",
        "@types/mocha": "^9.0.0",
        "chai": "^4.3.4",
        "mocha": "^9.0.3",
        "ts-mocha": "^10.0.0",
        "typescript": "^4.3.5"
    }
}
